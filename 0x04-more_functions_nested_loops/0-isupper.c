#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int _isupper(int c)
{
	char c;
	
	if(c <= 'Z')
	{
		printf("%C" is uppercase);
	}

	else if (c <=z )
	{
		printf("%c" is lowercase);
		return (0);
	}
}
